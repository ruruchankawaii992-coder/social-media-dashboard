# Server
PORT=4000
NODE_ENV=development

# Authentication
JWT_SECRET=smm-dev-secret-key-2026

# Database (PostgreSQL)
SMM_DATABASE_URL=postgresql://smm_user:smm_pass@localhost:5432/smm_dashboard

# Mock database mode — set to 'true' to use in-memory mock instead of PostgreSQL
# SMM_USE_MOCK=true
# USE_MOCK_DB=true
